// check if in the past the user already has a chat associated with this paper
// if so, load the chat with the chatId or paperId
// if not, create a new chat with the paperId and router.push to the chat page. do I need the create chat UI?

// The "start chatting" button should not immediately direct to a specific URL until you have determined whether there is an existing chat or a new one needs to be created. Instead, the button should trigger a function that performs the check and then directs the user to the appropriate URL based on the outcome.
